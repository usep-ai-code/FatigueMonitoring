# Production Environment Configuration
# Update this URL to your production backend API
# When deployed together with backend, use same origin (empty = same origin)
# VITE_API_URL will fallback to window.location.origin in config.js
VITE_API_URL=
